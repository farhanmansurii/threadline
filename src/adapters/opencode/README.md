# OpenCode Adapter

Responsibilities:

- Install Threadline skills into `~/.opencode/skills`
- Install commands into `~/.opencode/commands`
- Manage `~/.opencode/config.toml` with a Threadline-managed block
- OpenCode rules/config integration (planned)

Implemented in `adapter.mjs`.
