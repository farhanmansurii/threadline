{
  "name": "threadline",
  "version": "0.1.0",
  "description": "Portable agent environment manager for Claude Code, Codex, Cursor, OpenCode, Kimi, RAG, Obsidian handoffs, and project-aware workflows.",
  "type": "module",
  "bin": {
    "threadline": "./bin/threadline"
  },
  "scripts": {
    "check": "node --check bin/threadline && node --check src/cli.mjs && node --check src/core/detect-project.mjs && node --check src/core/execute.mjs && node --check src/core/paths.mjs && node --check src/core/plan.mjs && node --check src/core/skills.mjs && node --check src/core/templates.mjs",
    "smoke": "node bin/threadline detect --path examples/fullstack-js --json && node bin/threadline skills list && node bin/threadline skills recommend --path examples/fullstack-js && node bin/threadline setup --dry-run --runtimes claude,codex && node bin/threadline init --path examples/fullstack-js --dry-run",
    "test": "node --test"
  },
  "keywords": [
    "claude-code",
    "codex",
    "cursor",
    "opencode",
    "kimi",
    "rag",
    "obsidian",
    "mcp",
    "ai-agents",
    "developer-tools"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
